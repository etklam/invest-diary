// Compatibility alias for clients that conceptually address the canonical
// Diary-rooted feed. The response is the same owner-scoped read model.
export { default } from '~/server/api/investment-activity.get'
